{
  "name": "backend",
  "version": "1.0.0",
  "description": "A backend server to service database requests to practice manipulating a table with foreign key constraints",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jeffrey Tan",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mysql2": "^2.2.5"
  }
}
