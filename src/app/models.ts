export interface OrderDetails {
    id: number,
    order_date: string,
    customer_id: number,
    total_quantity: string,
    total_unit_price: string,
    total_discount: number,
    cost_price: string
}